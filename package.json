{
  "name": "motion-mobx-router",
  "version": "1.1.2",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -r lib; babel src --out-dir lib",
    "watch": "npm run build -- --watch"
  },
  "author": "Motion (motion.io)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-steelbrain": "^4.0.2"
  },
  "dependencies": {
    "history": "^4.5.1",
    "mobx": "^3.1.0",
    "url-pattern": "^1.0.3"
  }
}
